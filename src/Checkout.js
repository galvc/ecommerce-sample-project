import React, { Component } from "react";

class Checkout extends Component {
    render() {
        return (
            <div>
                <button> Checkout button from component </button>
            </div>
        );
    }
}

export default Checkout;
